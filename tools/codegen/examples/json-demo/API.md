# Json API Reference

**Function prefix:** `json_`

## Functions

| Function | Description |
|----------|-------------|
| `json_parse(json_str)` → String | Parse a JSON string into internal representation |
| `json_is_valid(json_str)` → Boolean | Check if a string is valid JSON |
| `json_prettify(json_str)` → String | Pretty print JSON with indentation |
| `json_minify(json_str)` → String | Minify JSON (remove whitespace) |
| `json_get_string(json_str, path)` → String | Get a string value at path (e.g., "user.name" or "items.0.id") |
| `json_get_number(json_str, path)` → Number | Get a number value at path |
| `json_get_int(json_str, path)` → Number | Get an integer value at path |
| `json_get_bool(json_str, path)` → Boolean | Get a boolean value at path |
| `json_get_json(json_str, path)` → String | Get a nested object/array as JSON string at path |
| `json_has_path(json_str, path)` → Boolean | Check if a path exists in JSON |
| `json_get_type(json_str, path)` → String | Get the type of value at path ("string", "number", "boolean", "array", "object", "null", "undefined") |
| `json_array_len(json_str, path)` → Number | Get array length at path |
| `json_array_get(json_str, path, index)` → String | Get array element at index (path can be empty for root array) |
| `json_object_keys(json_str, path)` → String | Get all keys of an object at path (returns JSON array of strings) |
| `json_object_len(json_str, path)` → Number | Count keys in object at path |
| `json_set_string(json_str, path, value)` → String | Set a string value at path |
| `json_set_number(json_str, path, value)` → String | Set a number value at path |
| `json_set_bool(json_str, path, value)` → String | Set a boolean value at path |
| `json_set_null(json_str, path)` → String | Set null at path |
| `json_set_json(json_str, path, value_json)` → String | Set a JSON value at path (parses the value string as JSON) |
| `json_delete_path(json_str, path)` → String | Delete a key at path |
| `json_new_object()` → String | Create an empty JSON object |
| `json_new_array()` → String | Create an empty JSON array |
| `json_array_push(json_str, value_json)` → String | Append value to array (value is JSON string) |
| `json_merge(json1, json2)` → String | Merge two JSON objects (second overwrites first on conflicts) |
| `json_encode(list [, pretty])` | Convert Ring list to JSON string |
| `json_decode(json_string)` | Convert JSON string to Ring list |

---
*Generated by gendoc.ring*
