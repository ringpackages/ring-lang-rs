# Uuid API Reference

**Function prefix:** `uuid_`

## Functions

| Function | Description |
|----------|-------------|
| `uuid_v4()` → String | Generate a random UUID v4 |
| `uuid_v7()` → String | Generate a UUID v7 (time-ordered, random) |
| `uuid_nil()` → String | Generate a nil UUID (all zeros) |
| `uuid_max()` → String | Generate a max UUID (all ones) |
| `uuid_parse(s)` → String | Parse a UUID string (returns canonical format or empty on error) |
| `uuid_is_valid(s)` → Boolean | Check if string is a valid UUID |
| `uuid_get_version(s)` → Number | Get UUID version (0 for nil, 4 for random, 7 for time-ordered, etc.) |
| `uuid_get_variant(s)` → String | Get UUID variant |
| `uuid_to_string(s)` → String | Format UUID as lowercase hyphenated (canonical) |
| `uuid_to_upper(s)` → String | Format UUID as uppercase hyphenated |
| `uuid_to_simple(s)` → String | Format UUID without hyphens |
| `uuid_to_urn(s)` → String | Format UUID as URN (urn:uuid:...) |
| `uuid_to_braced(s)` → String | Format UUID as braced ({...}) |
| `uuid_equals(uuid1, uuid2)` → Boolean | Check if two UUIDs are equal |
| `uuid_is_nil(s)` → Boolean | Check if UUID is nil (all zeros) |
| `uuid_is_max(s)` → Boolean | Check if UUID is max (all ones) |
| `uuid_compare(uuid1, uuid2)` → Number | Compare two UUIDs (-1 if first < second, 0 if equal, 1 if first > second) |
| `uuid_to_hex(s)` → String | Get UUID as hex-encoded bytes (32 chars) |
| `uuid_from_hex(hex_str)` → String | Create UUID from hex-encoded bytes |
| `uuid_batch_v4(count)` → String | Generate multiple v4 UUIDs (returns JSON array) |
| `uuid_batch_v7(count)` → String | Generate multiple v7 UUIDs (returns JSON array) |
| `uuid_v7_timestamp_ms(s)` → Number | Extract Unix timestamp (milliseconds) from v7 UUID |
| `uuid_v7_from_timestamp_ms(timestamp_ms)` → String | Create v7 UUID from Unix timestamp (milliseconds) |

---
*Generated by gendoc.ring*
