# Dt API Reference

**Function prefix:** `dt_`

## Functions

| Function | Description |
|----------|-------------|
| `dt_now_utc()` → String | Get current UTC timestamp (RFC 3339 format) |
| `dt_now_local()` → String | Get current local timestamp (RFC 3339 format) |
| `dt_now_unix()` → Number | Get current Unix timestamp (seconds since epoch) |
| `dt_now_unix_ms()` → Number | Get current Unix timestamp in milliseconds |
| `dt_parse_rfc3339(s)` → String | Parse RFC 3339 datetime string (e.g., "2024-01-15T10:30:00Z") |
| `dt_parse_format(s, format)` → String | Common formats: "%Y-%m-%d %H:%M:%S", "%d/%m/%Y", "%Y-%m-%d" |
| `dt_parse_date(s, format)` → String | Parse date only (returns "YYYY-MM-DD") |
| `dt_from_unix(timestamp)` → String | Parse Unix timestamp to RFC 3339 |
| `dt_from_unix_ms(timestamp_ms)` → String | Parse Unix timestamp in milliseconds to RFC 3339 |
| `dt_format_datetime(datetime, format)` → String | Format datetime with custom format string |
| `dt_to_rfc3339(datetime)` → String | Convert to RFC 3339 format |
| `dt_to_rfc2822(datetime)` → String | Convert to RFC 2822 format (email date format) |
| `dt_to_unix(datetime)` → Number | Convert to Unix timestamp |
| `dt_get_year(datetime)` → Number | Get year from datetime |
| `dt_get_month(datetime)` → Number | Get month (1-12) from datetime |
| `dt_get_day(datetime)` → Number | Get day of month (1-31) from datetime |
| `dt_get_hour(datetime)` → Number | Get hour (0-23) from datetime |
| `dt_get_minute(datetime)` → Number | Get minute (0-59) from datetime |
| `dt_get_second(datetime)` → Number | Get second (0-59) from datetime |
| `dt_get_weekday(datetime)` → Number | Get day of week (1=Monday, 7=Sunday) |
| `dt_get_weekday_name(datetime)` → String | Get day of week name |
| `dt_get_day_of_year(datetime)` → Number | Get day of year (1-366) |
| `dt_get_week_number(datetime)` → Number | Get ISO week number (1-53) |
| `dt_add_days(datetime, days)` → String | Add days to datetime |
| `dt_add_hours(datetime, hours)` → String | Add hours to datetime |
| `dt_add_minutes(datetime, minutes)` → String | Add minutes to datetime |
| `dt_add_seconds(datetime, seconds)` → String | Add seconds to datetime |
| `dt_add_weeks(datetime, weeks)` → String | Add weeks to datetime |
| `dt_add_months(datetime, months)` → String | Add months to datetime (handles month boundaries) |
| `dt_add_years(datetime, years)` → String | Add years to datetime |
| `dt_diff_days(datetime1, datetime2)` → Number | Get difference between two datetimes in days |
| `dt_diff_hours(datetime1, datetime2)` → Number | Get difference between two datetimes in hours |
| `dt_diff_minutes(datetime1, datetime2)` → Number | Get difference between two datetimes in minutes |
| `dt_diff_seconds(datetime1, datetime2)` → Number | Get difference between two datetimes in seconds |
| `dt_is_before(datetime1, datetime2)` → Boolean | Check if datetime1 is before datetime2 |
| `dt_is_after(datetime1, datetime2)` → Boolean | Check if datetime1 is after datetime2 |
| `dt_is_same_day(datetime1, datetime2)` → Boolean | Check if two datetimes are the same day |
| `dt_create(year, month, day, hour, minute, second)` → String | Create datetime from components |
| `dt_create_date(year, month, day)` → String | Create date only |
| `dt_start_of_day(datetime)` → String | Get start of day (00:00:00) |
| `dt_end_of_day(datetime)` → String | Get end of day (23:59:59) |
| `dt_is_valid(datetime)` → Boolean | Check if string is valid datetime |
| `dt_is_leap_year(year)` → Boolean | Check if year is leap year |
| `dt_days_in_month(year, month)` → Number | Get days in month |

---
*Generated by gendoc.ring*
