# Hash API Reference

**Function prefix:** `hash_`

## Functions

| Function | Description |
|----------|-------------|
| `hash_base64_encode(input)` → String | Encode a string to Base64 |
| `hash_base64_decode(input)` → String | Decode a Base64 string |
| `hash_hex_encode(input)` → String | Encode a string to hexadecimal |
| `hash_hex_decode(input)` → String | Decode a hexadecimal string |
| `hash_md5_hash(input)` → String | Compute MD5 hash of a string |
| `hash_sha256_hash(input)` → String | Compute SHA-256 hash of a string |
| `hash_sha512_hash(input)` → String | Compute SHA-512 hash of a string |

## Classes

Classes are generated Ring wrappers around the underlying structs.

```ring
load "hash_classes.ring"
```

### Hasher Class

Hasher class - configurable hash algorithm

**Constructor**

```ring
obj = new Hasher(algorithm)
```

**Properties**

| Property | Type | Getter | Setter |
|----------|------|--------|--------|
| algorithm | String | `obj.algorithm()` | `obj.setAlgorithm(value)` |

**Methods**

| Method | Description |
|--------|-------------|
| `obj.hash(input)` → String | Hash input string using the configured algorithm |

**Destructor**

```ring
obj.delete()
```

**Example**

```ring
load "hash_classes.ring"

obj = new Hasher("example")
? obj.algorithm()    # Get algorithm
? obj.hash("test")
obj.delete()
```

---
*Generated by gendoc.ring*
