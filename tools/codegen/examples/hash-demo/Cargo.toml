[package]
name = "ring_hash"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
ring-lang-rs = "0.1"
base64 = "0.22"
sha2 = "0.10"
md-5 = "0.10"
hex = "0.4"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
